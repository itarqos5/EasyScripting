# EasyScripting architecture

The repository was empty on 2026-09-13. No pre-existing code, build or repository instructions were present.

* Target matrix: Paper/Purpur 26.2 primary; 26.1, 1.21.11, 1.21.8 compatibility candidates. JDK 25 toolchain, Java 21 bytecode, shared public Paper APIs, server-provided Adventure. No NMS or Folia claim.
* Build/artifact: Gradle Kotlin DSL, single deployable plugin and sources JAR, no bundled Paper/Citizens/voice chat classes. Version expansion in source plugin.yml.
* Plugin model: conventional plugin.yml; EasyScriptingPlugin owns constructor wiring and enable/disable.
* Lifecycle: validate and load files during enable; register listeners, commands and public service after initialization. Disable stops producers, restores temporary state, removes owned actors, and drains writes.
* Registration: /es namespace, /scene and /actor aliases through dedicated routers; event listeners own gameplay restrictions and inventory protection.
* State: schema-versioned YAML definitions and runtime stores, validated identifiers, atomic replacement, bounded single-writer executor. Invalid source files are preserved and diagnosed.
* Thread model: synchronous entity/world access; Bukkit values are detached on the owning thread, then YAML encoding and atomic disk replacement run on a bounded writer. Large region captures create immutable plain records incrementally. Chat/voice callbacks read immutable/concurrent snapshots. Definition reads happen during startup or explicit administrative reload.
* Integrations: optional Citizens for real player NPCs; optional Simple Voice Chat for microphone moderation. Their classes are loaded only when dependencies are present. Core mob actors have no external runtime dependency.
* Validation: unit tests for pure scheduling/validation/serialization, build and artifact checks, exact-version compilation, isolated server startup and manual multiplayer acceptance.
* Risks: packet-sensitive NPC and skin behavior depends on the matching Citizens version. Server-side camera motion is bounded by vanilla client interpolation. Public screenshots show feature availability, not all hidden options.

## Implemented modules

1. `core`, `config`, `storage`, `api`: validated settings, feature registry, message rendering, atomic YAML, immutable scene definitions and deterministic tick cursor.
2. `actors`, `integration`: owned actor registry and backend interface, mob and Citizens adapters, transforms/equipment/navigation, lifecycle cleanup, recording paths.
3. `scenes`, `players`: action registry, resource locking, timelines, snapshot/reset, recording sessions and disconnect/world-unload cancellation.
4. `items`, `world`, `utilities`: kits, warps, rollback, environment and production controls, opt-in moderation/protection.
5. `commands`, `gui`: permission-gated modules and configurable inventory holders with cancellation, pagination, confirmations and editing.
6. Tests and documentation follow each coherent group. API events run synchronously; saves capture state before dispatch. Failed writes are reported; disabling invalidates late callbacks.

## Ownership and bounded work

### 0.1.5 implementation

Preserve the single Gradle artifact, JDK 25 toolchain/Java 21 bytecode, conventional plugin.yml, shared Paper 1.21.8 API with an affected-server 1.21.11 compile gate, optional Citizens/voice integrations, main-thread entity ownership and bounded YAML writer. No Folia claim or NMS. The working tree starts clean at 0ccfe0b. Existing sources have no applicable AGENTS.md or contribution rules. Unit tests and smoke-source compilation remain the validation gate; the user does not want a test server started.

* ActorService/ActorDefinition: retain normal hit/knockback processing for immortal actors by limiting lethal damage above a health floor; cancel death at LOWEST as a fallback. Citizens removes fake players after die(), so preventing lethal damage is the primary path. Keep the independent melee-only Hittable rule. New actors default to mortal; a one-time, backed-up config migration changes the old creation default while preserving individual saved actor choices. Announce actual NPC deaths using a configurable leave message.
* CitizensBackend/ActorBackend: persist a player-NPC tab-list setting and apply it to current and future viewers. Remove Citizens' extra spawn no-damage ticks, preserve runtime state/equipment and rebind owned entity IDs across Citizens identity refreshes so a running replay continues after name/skin edits. Keep Citizens optional and clean up its listeners/refresh snapshots with its registry.
* RecordingService/PlaybackCursor/SceneCommands/MenuService: allow identity and mode edits during replay, change cursor end behavior without restarting the current frame, retain selected button styling, and expose play/autoplay/continuous repeat/stop clearly. Manual Stop disables autoplay and preserves the current location. Reapply the actor's current presentation after an old take snapshot is restored. Acting/scene leases still protect operations that would corrupt captured state.
* IdentityService and a bounded username API client: implement /nickname <online-real-player-or-nick>, /nickname <player-or-nick> off and /nickname off, plus namespaced access and permission checks. Reuse Paper profile/display/tab APIs, preserve skins and original profile/display components, reserve aliases case-insensitively, reject NPCs, and clear nicknames on disconnect/reconnect. Resolve generated usernames asynchronously through Random User's documented HTTPS API, with bounded response/timeout/concurrency, no player data in requests, configurable local fallback, and session/request checks before returning to the main thread. Shutdown/quit/reset invalidate pending requests.
* Join/quit/death components: use nicknames for victim and killer names without replacing unrelated text, preserve null/suppressed messages, and restore the real identity on the next connection. Temporary acting and player-state restoration must not reintroduce an expired nickname. Document vanilla client/account-name limits without promising a client modification.
* Settings, YAML, descriptor, docs and tests: add nicknames.yml, messages and GUI leaves; preserve custom GUI slots and text where possible; keep only README.md in root. Cover cursor transitions, mortal defaults/migration, nickname response validation/collision/session state and event behavior using meaningful unit tests. Build and inspect the artifact, push and publish 0.1.5.

Official sources: [Paper profile API](https://jd.papermc.io/paper/1.21.11/org/bukkit/entity/Player.html#setPlayerProfile(com.destroystokyo.paper.profile.PlayerProfile)), [Paper death cancellation](https://jd.papermc.io/paper/1.21.11/org/bukkit/event/entity/EntityDeathEvent.html), [Citizens event ordering](https://github.com/CitizensDev/Citizens2/blob/master/main/src/main/java/net/citizensnpcs/EventListen.java), [Random User API](https://randomuser.me/documentation). Existing server APIs supply profile and JSON support; no new external runtime plugin or library is required.

Implementation refinements: Citizens' EntityHumanNPC schedules removal after `die()` even when Paper cancels the event. Immortal actors therefore receive positive nonlethal damage, with a minimal health buffer at the half-heart floor; death cancellation remains a fallback. Chat blocking uses Paper's synchronous ChatEvent to read live operator status safely; the documented deprecation/extra main-thread handoff is accepted here because a cached permission result caused the reported de-op bypass. The handler is bounded and does no I/O.

Additional kit scope: expose `/es kits`, a kit details page, blank creation, inventory import and explicit editor save/apply. KitImports reads item exports from installed PlayerKits2, PlayerKits (legacy), EssentialsX and CMI without granting a kit or invoking claim/reward commands. This adapter deliberately uses reflection against a fixed set of verified public provider methods, isolated from game internals: providers are optional, their jars are not redistributed, unsupported signatures produce an actionable error, and item metadata is converted by the provider itself. Test adapters against contract fixtures and item-capacity/collision behavior; actual installed-provider runtime acceptance remains unrun. Add an EasyScripting YAML export/import format and inventory capture as the universal option for other providers. Defaults/limits and all GUI controls remain YAML-configurable.

Provider sources: [PlayerKits2](https://github.com/Ajneb97/PlayerKits2), [PlayerKits legacy](https://github.com/Ajneb97/PlayerKits), [EssentialsX Kit](https://github.com/EssentialsX/Essentials/blob/2.x/Essentials/src/main/java/com/earth2me/essentials/Kit.java), [CMI API](https://www.zrips.net/cmi/api/).

### 0.1.4 implementation

Preserve the single Gradle plugin, conventional plugin.yml, JDK 25/Java 21 bytecode, compile-only Paper 1.21.8–26.2 APIs, optional Citizens/voice and main-thread game-state ownership. No new dependency, NMS or Folia claim. The deployable artifact remains EasyScripting; smoke code remains separate. The reported shutdown failure is from Paper 1.21.11, where ActorService.save starts a behavior timer after the plugin is disabled.

* TickEngine and the entrypoint: stop accepting new jobs before service cleanup, check plugin enablement, and avoid retaining jobs when scheduler registration fails. ActorService and RecordingService must not queue behavior/autoplay during restoration or shutdown. Use deterministic scheduler tests plus the build; no test server, following the existing user instruction.
* ActorService: observe uncancelled deaths at MONITOR, mark actors unavailable immediately, remove the saved definition through YamlStore, then release scene/replay ownership and backend resources on the following main-thread tick. Suppress owned drops separately at HIGHEST. Pending deletion cannot be saved or respawned, including during shutdown. SceneService must skip permanently deleted actors and avoid applying old snapshots to a reused ID. Actual scripted deaths also delete the actor. Acting also waits for an uncancelled death before ending the performance; Paper supports [cancelling deaths](https://jd.papermc.io/paper/1.21.8/org/bukkit/event/entity/EntityDeathEvent.html).
* Actor damage: Hittable controls direct melee/sweep attacks only. Environmental damage, projectiles and explosions remain eligible; Immortal still blocks lethal damage. Knockback filtering must make the same distinction, and replay recovery must respond to allowed impacts even with Hittable OFF. Acting performers block direct melee only, permit environmental/projectile damage and restore their prior invulnerability state afterward. Reuse Paper's public damage and knockback causes.
* ModerationService/Messages/Settings/YAML: broadcast text also appears as an Adventure title. Announce real mute/unmute transitions to chat. New title templates and timing defaults must work on existing installations without replacing custom messages. Keep all audience and mutation calls on the command/main thread; the existing async chat handler only reads cached moderation state.
* Update GUI help, usage/configuration/changelog, relevant fixtures and the version. Validate damage categories, title defaults/customization, scheduler shutdown, compile against the affected Paper 1.21.11 API, build/unit tests, and inspect the JAR. Native combat/title behavior remains unrun in-game. Damage categories follow the [Paper damage causes](https://jd.papermc.io/paper/1.21.8/org/bukkit/event/entity/EntityDamageEvent.DamageCause.html); titles use [Adventure](https://docs.papermc.io/adventure/titles/).

### 0.1.3 implementation

Preserve the target/build/plugin/thread/integration matrix above: compile-only Paper 1.21.8 through 26.2, JDK 25/Java 21 bytecode, conventional plugin.yml, synchronous entity access, bounded YAML writer, optional Citizens and voice, no Folia claim or new dependency. The repository was published at 0.1.2 before this work.

* MenuService/MenuHolder and guis.yml: replace the inventory presentation and navigation, add readable states, contextual empty screens and selectors. Settings upgrades pre-v2 GUI files only after validation and preserves the original as a backup; subsequent v2 customization remains authoritative. GuiSchema validates reachable control slots, navigation and layouts.
* ActorDefinition/ActorService/SceneCommands: persist autoplay, apply the configurable default and expose a per-actor command/toggle. Spawn notifications feed RecordingService's bounded one-shot autoplay queue, including existing actors at startup. Explicit stop cancels queued starts; occupied, hidden, deleted, disabled and dead actors cannot start.
* ActingService: cancel performer damage/knockback while capturing; successful saves request autoplay only after restoring the performer/NPC. Cancel and shutdown never start a new replay.
* RecordingService and a pure ReplayRecovery helper: let native knockback run for a bounded interval with gravity, then blend movement back to the paused frame. Health lost during playback survives stop/reset; death cancels playback without resurrecting the actor. All callbacks stay on the main thread and use the existing TickEngine; close prevents queued work from restarting.
* Validation: unit tests for migration, GUI slot conflicts and recovery timing; full Gradle build, Paper 26.2 API compilation and artifact inspection. No test server startup, following the user's request. The supported knockback event is checked against the [1.21.8 API](https://jd.papermc.io/paper/1.21.8/io/papermc/paper/event/entity/EntityKnockbackEvent.html) and [26.2 API](https://jd.papermc.io/paper/26.2/io/papermc/paper/event/entity/EntityKnockbackEvent.html); graphical/combat runtime behavior and autoplay lifecycle remain unrun acceptance checks for 0.1.3. Citizens NPCs explicitly enable its [knockback metadata](https://github.com/CitizensDev/CitizensAPI/blob/master/src/main/java/net/citizensnpcs/api/npc/NPC.java); Paper event cancellation enforces the Hittable switch.

0.1.2 extends RecordingService with explicit end modes and equipment/pose frames while reading legacy recordings. ActingService owns temporary performer possession using PlayerService snapshots, actor reservations and the shared tick engine. ActorDefinition persists the selected recording/mode. MenuService provides actor sections and direct combat toggles backed by guis.yml. All entity/profile changes and event cleanup stay on the main thread; detached saves keep using YamlStore. Acting restores on finish/cancel/death/quit/disable and rejects conflicting scenes/cameras. No new production dependency, NMS or Folia support is introduced.

Acting profile changes use Paper's public `setPlayerProfile` API, which keeps the real player's UUID and changes the visible name/properties ([1.21.8 contract](https://jd.papermc.io/paper/1.21.8/org/bukkit/entity/Player.html#setPlayerProfile(com.destroystokyo.paper.profile.PlayerProfile)), [26.2 contract](https://jd.papermc.io/paper/26.2/org/bukkit/entity/Player.html#setPlayerProfile(com.destroystokyo.paper.profile.PlayerProfile))). The original profile is serialized only for acting checkpoints; ordinary scene/take snapshots retain their existing identity contract. The actor is suspended without persisting a temporary hidden flag, so a restart loads its saved definition. Actor playback Stop releases ownership, holds the current frame and disables autoplay; the legacy recording command retains its starting-snapshot restoration behavior. A pure PlaybackCursor defines endpoint order, including single-frame recordings.

Citizens equipment is initialized through its Equipment trait, and immediate name/skin edits capture current equipment before Citizens can respawn the entity ([Equipment implementation](https://github.com/CitizensDev/CitizensAPI/blob/master/src/main/java/net/citizensnpcs/api/trait/trait/Equipment.java)). Recording playback uses Paper's visual-fire state for flame cues, restoring its previous value on completion; it never converts recorded flames into actual fire damage ([Entity API](https://jd.papermc.io/paper/1.21.8/org/bukkit/entity/Entity.html#setVisualFire(net.kyori.adventure.util.TriState))).

Scenes reserve entity/world targets. Movement playback shares actor reservations, preventing competing timelines. Spawned actors hold reference-counted plugin chunk tickets; cleanup releases them. Temporary projectiles and boats belong to their creating services. A single tick engine runs only while jobs exist.

NPC identity pools are immutable validated configuration. Creation and explicit randomization choose from a bounded set after excluding occupied/blacklisted names; IDs remain stable. Name and skin owner are separate persisted fields. Only the Citizens adapter touches `SkinTrait`: Citizens resolves profiles asynchronously, while EasyScripting captures completed texture/signature data on the existing actor behavior pulse every 20 ticks. Changed caches use the bounded YAML writer; no per-NPC timer or additional network executor is created. Saved signed textures are reapplied on spawn, and an explicit skin edit invalidates the cache. Legacy definitions without cached textures remain readable.

Region jobs allow one operation per world and use the concurrent-scene count as their global cap. Capture, full validation and block application have per-tick budgets. Coordinates, block data, container items and sign text are validated before the first mutation. Loaded-chunk checks prevent implicit synchronous loads. Cancellation/unload stops further writes; a multi-tick restore is not a transaction and does not undo already applied blocks. Containers use snapshot inventories before block-state updates.

Reload validates a candidate configuration before publication. Deferred player snapshots survive graceful disconnect/death; active take snapshots remain memory-only until deferral. A process crash cannot guarantee recovery of active takes. Test fixtures are separate artifacts and never included in the production JAR.

Identity cleanup runs after player/acting/scene restoration so old snapshots cannot leave temporary names on disable. Player snapshots taken under a nickname carry original display/tab components, allowing restoration after reset/reconnect without reintroducing the alias or stripping unrelated custom formatting. Citizens pending identity respawns pause the replay cursor for a bounded 100 ticks; Stop during that interval attaches its cleanup to the replacement entity. Spawn equipment is synchronized before Citizens runs its traits. The existing shared TickEngine owns all playback and username completion checks.
